import styled from 'styled-components'

const RaidsStyle = styled.main`
color: #FFF;
`

const Raids = () => {
    return (
        <RaidsStyle>
        Not ready yet
        </RaidsStyle>
    );
}

export default Raids;
